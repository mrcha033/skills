interface:
  display_name: "Quant Stock Polling Trader"
  short_description: "KIS/Toss polling and systemd supervision"
  default_prompt: "Use $quant-stock-polling-trader to securely guide KIS credential setup, verify token authentication, prepare fail-closed polling, and generate or explicitly activate verified systemd jobs."
